class GameScreen extends Container {
  constructor(game, controls) {
    super();
  // Initialization code
  }
  update(dt, t) {
  super.update(dt, t);
  this.level = level;
  this.camera = camera;
  this.squizz = squizz;
  // Game screen update logic
  }
}
